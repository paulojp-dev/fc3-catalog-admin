spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: root
    password: catalog_admin
    url: jdbc:h2:mem:catalog_admin_test_db;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
  h2:
    console:
      enabled: true
      path: /h2